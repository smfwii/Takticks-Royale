using UnityEngine;
public class FromClientClass : MonoBehaviour {
    /*This is just a way to differentiate between the host's troops and the client's troops.
     There might be a better way to do this, but I ain't finding it.
    */
}
